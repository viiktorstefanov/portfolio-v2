import React from 'react';
import { menuItems } from '../../constants/data';

const Skills: React.FC = () => {
  return (
    <div className='flex flex-col justify-center relative z-10 items-center pt-20' id={menuItems[1].href}>
      
    </div>
  );
};

export default Skills;